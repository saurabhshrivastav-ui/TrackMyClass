# TrackMyClass Backend (MongoDB)

## Setup
1. Copy `.env.example` to `.env` and fill `MONGODB_URI`.
2. Install deps: `npm install`
3. Seed data: `npm run seed`
4. Start server: `npm run dev`

## API
- `GET /api/students/:studentId/overview`
- `POST /api/students`

